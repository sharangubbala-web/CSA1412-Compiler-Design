%{
#include <stdio.h>
%}

%%

[0-9]+              { printf("%s is DIGIT\n", yytext); }

[0-9]*\.[0-9]+      { printf("%s is DIGIT\n", yytext); }

.+                  { printf("%s is NOT A DIGIT\n", yytext); }

%%

int yywrap()
{
    return 1;
}

int main()
{
    printf("Enter the input: ");
    yylex();

    return 0;
}
