%{
#include <stdio.h>

int ln = 0;
%}

%%

.*\n    {
            ln++;
            fprintf(yyout, "%d: %s", ln, yytext);
        }

%%

int yywrap()
{
    return 1;
}

int main()
{
    yyin = fopen("simple.txt", "r");
    yyout = fopen("out.txt", "w");

    if (yyin == NULL)
    {
        printf("Cannot open simple.txt\n");
        return 1;
    }

    yylex();

    fclose(yyin);
    fclose(yyout);

    return 0;
}
