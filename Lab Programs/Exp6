%{
#include <stdio.h>
%}

%%

\<[^>]*\>    { fprintf(yyout, "%s\n", yytext); }

.|\n         ;

%%

int yywrap()
{
    return 1;
}

int main()
{
    yyin = fopen("sample.html", "r");
    yyout = fopen("output.txt", "w");

    if (yyin == NULL)
    {
        printf("Cannot open sample.html\n");
        return 1;
    }

    yylex();

    fclose(yyin);
    fclose(yyout);

    return 0;
}
